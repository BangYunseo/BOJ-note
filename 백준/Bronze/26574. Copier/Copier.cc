#include <iostream>

using namespace std;

int main(){
    int T, temp;
    cin >> T;
    
    for(int i = 0; i < T; i++){
        cin >> temp;
        cout << temp << ' ' << temp << endl;
    }
}