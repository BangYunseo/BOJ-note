#include <iostream>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int n, count, m = 2, k = 1;
    cin >> n;
    
    int arr[n];
    for(int i = 0; i < n; i++) cin >> arr[i];
    
    cout << 1 << ' ' << 0;
}