#include <iostream>

using namespace std;

int main(){
    int T;
    cin >> T;
    
    int N, M;
    while(T--){
        cin >> N >> M;
        int a, b;
        while(M--){
            cin >> a >> b;
        }
        cout << N - 1 << endl;
    }
}